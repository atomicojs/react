{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run buil -w=@tomico/react && npm run build -w=next-app && npm publish -w=@atomico/react"
  },
  "dependencies": {
    "@atomico/run": "^0.10.1"
  }
}
